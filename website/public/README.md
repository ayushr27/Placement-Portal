## Public Assets

This directory contains static assets served as-is by the frontend (images, icons, PDFs, manifest, etc.).

- Files here are typically referenced by absolute paths at runtime
- Avoid importing from code; place only static files that do not require bundling


