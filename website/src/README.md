## Frontend Source (`src`)

This directory contains the application source code for the website frontend.

- `components/`, `admin/`, `student/`: UI components and feature modules
- `assets/`: local images/icons referenced by the app
- `main.jsx`, `App.jsx`: application entry and root component

Build and run instructions are documented in the repository root `README.md`.


