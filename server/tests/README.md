## Tests

This directory contains automated tests for the server application.

- Unit tests validate individual modules (services, routes, schemas)
- Integration tests verify components working together

Typical commands (see project root docs for exact tooling):
- Run tests: `pytest`
- Measure coverage: `pytest --cov`


