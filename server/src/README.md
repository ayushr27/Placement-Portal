## Server Source (`src`)

This is the main application source for the server.

- `main.py` (or equivalent entrypoint): starts the application
- `routes/`: API route handlers
- `services/`: business logic and integrations
- `schemas/`: request/response and data models
- `config.py`: configuration and environment handling

Run, build, and testing instructions are documented in the repository root `README.md`.


